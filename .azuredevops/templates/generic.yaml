parameters:
  - name: deployFromFeature
    type: boolean
    default: false
  - name: useCustomEnvironments
    type: boolean
  - name: program
    type: string
  - name: service
    type: string
  - name: resource
    type: string

extends:
  ${{ if eq(parameters.useCustomEnvironments, true) }}:
    template: services-infra-rtl.yaml
  ${{ if eq(parameters.useCustomEnvironments, false) }}:
    template: /infra/services/${{ parameters.program }}/${{ parameters.service }}/${{ parameters.resource }}.yaml
  parameters:
    deployFromFeature: ${{ parameters.deployFromFeature }}
    program: ${{ parameters.program }}
    service: ${{ parameters.service }}
    resource: ${{ parameters.resource }}
